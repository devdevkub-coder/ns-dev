# NS Scrap ERP Next.js App

This is a separate Next.js workspace for the new application direction. It does not import or execute `old-apps/vue` or `old-apps/legacy`.

## Commands

```bash
npm run next:dev
npm run next:build
```

Or run inside this folder:

```bash
npm install
npm run dev
```
